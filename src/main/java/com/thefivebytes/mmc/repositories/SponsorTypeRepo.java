package com.thefivebytes.mmc.repositories;

import com.thefivebytes.mmc.entities.SponsorType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SponsorTypeRepo extends JpaRepository<SponsorType,Long> {
}
